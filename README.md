Following packages installed to get the scraped list of records , also the data store use is Excel to import the list form search results.

The Excel will be created in the same solution directory

User Third part Nuget packages to get the search CsvHelper ,Html Agility Pack, HtmlAgilityPack.CssSelectors
